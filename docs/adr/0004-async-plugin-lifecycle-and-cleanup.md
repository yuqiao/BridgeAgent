---
status: accepted
---

# 从首版采用异步插件生命周期并汇总清理错误

阶段 1 的插件与宿主统一使用 async 生命周期，按依赖顺序串行激活；关闭或启动回滚时，逆序尝试所有已登记资源的释放，再汇总报告错误。用户已确认这一选择；启动失败与清理失败并存时保留两者，取消在清理后继续传播。

这比同步首版增加了取消与清理协调的实现成本，但后续模型连接、存储和进程资源可以直接使用同一生命周期。它也明确区别于 Cordis 某些清理路径的并行执行与日志容错：BridgeAgent 的调用方能观察到清理失败，而其他资源仍有机会释放。
