---
status: accepted
---

# 从第一版建立插件化架构分层

BridgeAgent 参考 DeepSeek Harness 构建原生 Python 本地编码助手。根据用户在 2026-09-25 的明确要求，先设计能力接口、插件宿主、具体能力实现与应用入口的职责和依赖方向，再在这些边界内实现最小 Agent 闭环；不采用先做耦合原型、后补插件架构的推进方式。

这使首个闭环需要承担最小装配和生命周期设计，但可以从起点验证替换模型、增加工具等能力不会迫使应用层或循环一起修改。分层使用同一个 Python 包内的模块实现，不要求照搬参考仓库的多包规模。
