# 阶段 3.5：基础 Skills

状态：实现与本地验收完成，交付检查已完成。基线 `stage3`，测试边界已在 [实施计划](03-07-delivery-plan.md) 中确认。使用见 [Skill 指南](../guides/skills.md)。

参考 DeepSeek Harness 的 `packages/skill/skill` 和 `packages/skill/tool-skill`：元数据目录、完整定义和模型工具分离。本项目首期使用一个显式文件 provider、重复名称拒绝和工具按需加载，不提前实现参考项目的多 provider 优先级、动态更新或完整事件日志投影。

| 编号 | 目标 | 验收证据 |
| --- | --- | --- |
| S01 | 发现只返回元数据，不向模型返回所有正文 | test_skills；test_skill_agent 中模型上下文始终不含未选正文标记 |
| S02 | 按名称加载正文，新增 Skill 无需改循环 | test_skills；真实 YAML 装配加载两份临时 Skill |
| S03 | 读取所属目录的资料，遵守共享文件策略 | 参考资料、符号链接逃逸、敏感路径与分页测试 |
| S04 | 无效元数据、重复名称、截断目录明确失败 | 元数据矩阵、重复名称、遍历限额测试 |
| S05 | Skill、参考资料、文件工具进入实际 Agent 流程 | test_skill_agent：四次真实工具调用后回答 source.py:1 |
| S06 | CLI 使用 Skill 且不授予写入/执行能力 | test_workspace_cli 的 Skill 参数用例；工具集合只包含六个只读工具 |
| S07 | 项目回归、静态检查和独立安装可用 | 144 项通过、2 项 live 默认跳过；Ruff/mypy/构建通过；独立 wheel 的 2 项仓库 CLI 测试通过 |

TDD 记录：从目录实现缺失开始；元数据校验、重复名称、load、read_resource、资料越界拒绝、文档符号链接范围、截断发现、YAML 插件清单、根目录预检和资料分页均先观察到失败，再最小实现。CLI 复用已有入口直接通过，作为验收而非伪造 red。

规范 review：SkillCatalog 和结果类型位于 contracts；文件读取通过 WorkspaceFiles；LangChain 类型仅出现在工具适配层；应用、宿主与 Agent 循环无需新增 Skill 特例。

目标 review：已覆盖 S01–S06。review 发现参考资料长于第一页时缺少续读参数，以及根目录的错误路径/重复项未提前拒绝，均补失败测试修复。资料与 SKILL.md 的符号链接必须解析后仍在所属目录；为 WorkspaceFiles.read 增加了默认不改变旧行为的 scope 参数。

限制：自然语言 Skill 不等于强制执行程序；超长正文拒绝并建议拆为资料；第三方 Skill 根目录暂限工作区内；真实模型工具验收的外部限流问题沿用阶段 2/3 状态，未宣布已通过。
